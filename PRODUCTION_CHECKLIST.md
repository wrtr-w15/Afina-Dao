# Чеклист подготовки к продакшену

## ✅ Проверка билда
- [x] Проект успешно собирается (`npm run build`)
- [x] Нет ошибок TypeScript
- [x] Нет ошибок линтера
- [x] Все страницы генерируются корректно

## 🔐 Переменные окружения (.env.local)

### Обязательные переменные для продакшена:

#### База данных
- [ ] `DB_HOST` - хост базы данных (не localhost для продакшена!)
- [ ] `DB_PORT` - порт MySQL (обычно 3306)
- [ ] `DB_USER` - пользователь базы данных
- [ ] `DB_PASSWORD` - **надежный пароль** базы данных
- [ ] `DB_NAME` - имя базы данных

#### Безопасность
- [ ] `ADMIN_PASSWORD` - **надежный пароль** для админ-панели
- [ ] `ADMIN_SESSION_SECRET` - **минимум 32 символа**, случайная строка
- [ ] `TELEGRAM_WEBHOOK_SECRET` - **минимум 32 символа**, случайная строка

#### Telegram Bot
- [ ] `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- [ ] `TELEGRAM_CHAT_ID` - ID чата для уведомлений

#### API и домен
- [ ] `NEXT_PUBLIC_API_URL` - **публичный URL** вашего сервера (https://yourdomain.com)
- [ ] `ALLOWED_ORIGIN` - ваш домен для CORS (https://yourdomain.com)
- [ ] `PORT` - порт приложения (обычно 3000)

#### Режим
- [ ] `NODE_ENV=production` - установлен в production

## 🗄️ База данных

- [ ] База данных создана и доступна
- [ ] Миграции применены:
  - [ ] `create_subscription_pricing.sql`
  - [ ] `create_pricing_settings.sql` (если используется)
- [ ] Таблицы созданы корректно
- [ ] Тестовое подключение успешно

## 🚀 Деплой

### Перед деплоем:
- [ ] Все зависимости установлены (`npm install`)
- [ ] Проект собран (`npm run build`)
- [ ] PM2 установлен глобально (`npm install -g pm2`)
- [ ] Папка `logs/` создана

### Запуск:
```bash
./start-production.sh
```

Или вручную:
```bash
cd frontend
npm run build
cd ..
pm2 start ecosystem.config.js
pm2 save
```

## 🔒 Безопасность

### Проверьте:
- [ ] Все пароли изменены с дефолтных значений
- [ ] `ADMIN_SESSION_SECRET` - случайная строка 32+ символов
- [ ] `TELEGRAM_WEBHOOK_SECRET` - случайная строка 32+ символов
- [ ] `.env.local` не добавлен в git (проверьте `.gitignore`)
- [ ] HTTPS настроен на сервере
- [ ] Firewall настроен (открыт только нужный порт)
- [ ] База данных доступна только с сервера приложения

## 📡 Telegram Webhook

После запуска приложения:
- [ ] Webhook настроен автоматически через скрипт
- [ ] Или настроен вручную через API Telegram
- [ ] Webhook URL: `https://yourdomain.com/api/telegram/webhook`
- [ ] Webhook secret token установлен

## 🌐 Домен и DNS

- [ ] Домен настроен и указывает на IP сервера
- [ ] SSL сертификат установлен (Let's Encrypt или другой)
- [ ] HTTPS работает корректно
- [ ] `NEXT_PUBLIC_API_URL` указывает на правильный домен

## 📊 Мониторинг

### PM2 команды:
```bash
pm2 status              # Статус приложения
pm2 logs afina-dao-frontend  # Логи
pm2 monit              # Мониторинг в реальном времени
pm2 restart afina-dao-frontend  # Перезапуск
```

### Проверка работы:
- [ ] Приложение отвечает на запросы
- [ ] Главная страница открывается
- [ ] API endpoints работают
- [ ] Админ-панель доступна
- [ ] Telegram бот отвечает

## 🔄 Обновление

Для обновления приложения:
```bash
git pull
cd frontend
npm install
npm run build
cd ..
pm2 restart afina-dao-frontend
```

## ⚠️ Важные замечания

1. **Никогда не коммитьте `.env.local` в git!**
2. **Используйте надежные пароли** для всех сервисов
3. **Регулярно обновляйте зависимости**: `npm audit` и `npm update`
4. **Делайте бэкапы базы данных** регулярно
5. **Мониторьте логи** на наличие ошибок
6. **Настройте автоматический перезапуск** через PM2 startup

## 🐛 Отладка

Если что-то не работает:
1. Проверьте логи: `pm2 logs afina-dao-frontend`
2. Проверьте статус: `pm2 status`
3. Проверьте переменные окружения
4. Проверьте подключение к базе данных
5. Проверьте доступность домена и SSL

## 📝 Дополнительные настройки

### Оптимизация Next.js:
- [x] `compress: true` - включено
- [x] `poweredByHeader: false` - включено
- [x] Security headers настроены
- [x] Image optimization настроена

### Рекомендации:
- Настройте CDN для статических файлов
- Настройте кэширование на уровне сервера
- Используйте reverse proxy (nginx) для лучшей производительности
- Настройте мониторинг (Sentry, LogRocket и т.д.)

